import { css } from '@emotion/react'

export const colorUtils = css`
  .bg-secondary { background-color: var(--bg-secondary); }
  .bg-tertiary { background-color: var(--bg-tertiary); }
  .border-primary { border: 1px solid var(--border-primary); }
`
